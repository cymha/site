---
import { type Locale, locales, t, toTarget } from '../i18n'

export interface Props {
  lang: Locale
  currentPath: string
}

const { lang, currentPath } = Astro.props

const labels: Record<Locale, string> = {
  en: t(lang).langSwitcher.en,
  'zh-cn': t(lang).langSwitcher.zhCN,
  'zh-hk': t(lang).langSwitcher.zhHK,
}
---

<div class="group relative">
  <button
    type="button"
    class="text-text-light hover:text-primary hover:bg-primary-light flex items-center gap-1.5 rounded-lg px-3 py-2 text-sm font-medium transition-colors"
    aria-haspopup="true"
  >
    <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M3 5h12M9 3v2m1.048 9.5A18.022 18.022 0 016.412 9m6.088 9h7M11 21l5-10 5 10M12.751 5C11.783 10.77 8.07 15.61 3 18.129"
      ></path>
    </svg>
    <span>{labels[lang]}</span>
    <svg class="h-3 w-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M19 9l-7 7-7-7"></path>
    </svg>
  </button>
  <div
    class="invisible absolute top-full right-0 z-50 mt-1 w-40 rounded-xl border border-gray-100 bg-white py-1 opacity-0 shadow-lg transition-all group-hover:visible group-hover:opacity-100 focus-within:visible focus-within:opacity-100"
  >
    {
      locales.map((l) => (
        <a
          href={toTarget(l, currentPath)}
          class={`lang-switcher-link block px-4 py-2 text-sm transition-colors ${
            l === lang
              ? 'text-primary bg-primary-light/50 font-semibold'
              : 'text-text-light hover:text-primary hover:bg-primary-light'
          }`}
          hreflang={l}
        >
          {labels[l]}
        </a>
      ))
    }
  </div>
</div>

<script>
  document
    .querySelectorAll('.lang-switcher-link')
    .forEach((el) =>
      el.addEventListener('click', () =>
        localStorage.setItem('cymha:lang', (el as HTMLAnchorElement).hreflang),
      ),
    )
</script>
